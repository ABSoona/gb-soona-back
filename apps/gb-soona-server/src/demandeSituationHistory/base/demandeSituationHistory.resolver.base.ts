/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { DemandeSituationHistory } from "./DemandeSituationHistory";
import { DemandeSituationHistoryCountArgs } from "./DemandeSituationHistoryCountArgs";
import { DemandeSituationHistoryFindManyArgs } from "./DemandeSituationHistoryFindManyArgs";
import { DemandeSituationHistoryFindUniqueArgs } from "./DemandeSituationHistoryFindUniqueArgs";
import { CreateDemandeSituationHistoryArgs } from "./CreateDemandeSituationHistoryArgs";
import { UpdateDemandeSituationHistoryArgs } from "./UpdateDemandeSituationHistoryArgs";
import { DeleteDemandeSituationHistoryArgs } from "./DeleteDemandeSituationHistoryArgs";
import { Demande } from "../../demande/base/Demande";
import { User } from "../../user/base/User";
import { DemandeSituationHistoryService } from "../demandeSituationHistory.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => DemandeSituationHistory)
export class DemandeSituationHistoryResolverBase {
  constructor(
    protected readonly service: DemandeSituationHistoryService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "DemandeSituationHistory",
    action: "read",
    possession: "any",
  })
  async _demandeSituationHistoriesMeta(
    @graphql.Args() args: DemandeSituationHistoryCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [DemandeSituationHistory])
  @nestAccessControl.UseRoles({
    resource: "DemandeSituationHistory",
    action: "read",
    possession: "any",
  })
  async demandeSituationHistories(
    @graphql.Args() args: DemandeSituationHistoryFindManyArgs
  ): Promise<DemandeSituationHistory[]> {
    return this.service.demandeSituationHistories(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => DemandeSituationHistory, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "DemandeSituationHistory",
    action: "read",
    possession: "own",
  })
  async demandeSituationHistory(
    @graphql.Args() args: DemandeSituationHistoryFindUniqueArgs
  ): Promise<DemandeSituationHistory | null> {
    const result = await this.service.demandeSituationHistory(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DemandeSituationHistory)
  @nestAccessControl.UseRoles({
    resource: "DemandeSituationHistory",
    action: "create",
    possession: "any",
  })
  async createDemandeSituationHistory(
    @graphql.Args() args: CreateDemandeSituationHistoryArgs
  ): Promise<DemandeSituationHistory> {
    return await this.service.createDemandeSituationHistory({
      ...args,
      data: {
        ...args.data,

        demande: {
          connect: args.data.demande,
        },

        creePar: args.data.creePar
          ? {
              connect: args.data.creePar,
            }
          : undefined,
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => DemandeSituationHistory)
  @nestAccessControl.UseRoles({
    resource: "DemandeSituationHistory",
    action: "update",
    possession: "any",
  })
  async updateDemandeSituationHistory(
    @graphql.Args() args: UpdateDemandeSituationHistoryArgs
  ): Promise<DemandeSituationHistory | null> {
    try {
      return await this.service.updateDemandeSituationHistory({
        ...args,
        data: {
          ...args.data,

          demande: {
            connect: args.data.demande,
          },

          creePar: args.data.creePar
            ? {
                connect: args.data.creePar,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => DemandeSituationHistory)
  @nestAccessControl.UseRoles({
    resource: "DemandeSituationHistory",
    action: "delete",
    possession: "any",
  })
  async deleteDemandeSituationHistory(
    @graphql.Args() args: DeleteDemandeSituationHistoryArgs
  ): Promise<DemandeSituationHistory | null> {
    try {
      return await this.service.deleteDemandeSituationHistory(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Demande, {
    nullable: true,
    name: "demande",
  })
  @nestAccessControl.UseRoles({
    resource: "Demande",
    action: "read",
    possession: "any",
  })
  async getDemande(
    @graphql.Parent() parent: DemandeSituationHistory
  ): Promise<Demande | null> {
    const result = await this.service.getDemande(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "creePar",
  })
  @nestAccessControl.UseRoles({
    resource: "User",
    action: "read",
    possession: "any",
  })
  async getCreePar(
    @graphql.Parent() parent: DemandeSituationHistory
  ): Promise<User | null> {
    const result = await this.service.getCreePar(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
